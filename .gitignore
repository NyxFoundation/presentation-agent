node_modules
.DS_Store
dist
*.local
.vite-inspect
.remote-assets
components.d.ts
*Zone.Identifier
actions-runner/
.env

# Generated artifacts. Maintainers force-add these only when publishing
# a deck on a `presentation/*` branch; contributors preview locally via
# `bun dev`.
outputs/
slides/
exports-*/
exports/

# Per-iteration history from the `make refine` visual self-improvement loop.
.refine/

# Ad-hoc PNG render dumps from local preview/debugging.
dist-png/
