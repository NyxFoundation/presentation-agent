{
  "name": "presentation-agent",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build && rm -f dist/_redirects",
    "dev": "slidev --open",
    "export": "CHROME=$(command -v google-chrome-stable || command -v chromium || command -v chrome || true); slidev export --per-slide ${CHROME:+--executable-path \"$CHROME\"}"
  },
  "dependencies": {
    "@slidev/cli": "^52.11.1",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "three": "^0.184.0",
    "vue": "^3.5.26"
  }
}