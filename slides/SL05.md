---
layout: default
theme: default
background: White
---

# L0層の技術詳細：1 Action = 1 Transaction で全履歴を検証可能に

- **エージェント状態**: 各エージェントは `(hp, skill, balance, reward_function)` などの状態を保持
- **決定ループ**: `(environment, rules, state)` を入力として次の `action` を決定
- **原子的アクション**: 1エージェントの action = 1ブロックチェーントランザクション
- **バリデーション**: Validatorがルール違反の検証・報酬計算を行い、新ブロックを生成
- **監査可能性**: 全txと状態遷移がオンチェーン記録され、第三者検証が可能

### なぜブロックチェーンか？（技術的根拠）
- **セキュリティ**: AIエージェントがサーバーハックを試みるリスクに対し、分散システムはハニーポットを分散できる
- **検証可能性**: 取引履歴・状態遷移・介入（ルール変更）が第三者にも検証可能
- **将来への布石**: 最終的な舞台がオンチェーン（公開ブロックチェーン）になることを見越した設計

```
┌─────────────┐    action     ┌───────────────┐   validate    ┌─────────────┐
│    Agent    │ ──────────▶  │  Transaction  │ ──────────▶  │  Validator  │
│ (state)     │              │               │              │             │
└─────────────┘              └───────────────┘              └──────┬──────┘
       ▲                                                           │
       │                    ┌───────────────┐                      │
       └────────────────────│  New Block    │◀─────────────────────┘
         updated state      │ + New State   │
                            └───────────────┘
```

![Visual: architecture_loop - Agent決定ループとブロックチェーン記録の図](https://via.placeholder.com/800x400.png?text=architecture_loop)
<!-- TODO: Replace with actual visual showing the Agent → Action (Tx) → Validator → New Block → Updated State loop -->

<!--
この技術アーキテクチャについてご意見をいただきたいと考えています。

各エージェントは状態を持ち、環境とルールを入力として次の行動を決定します。重要なのは、1つのアクションが1つのブロックチェーントランザクションとして記録されることです。これにより、すべての経済活動が検証可能な形で記録されます。

Validatorはルール違反がないか、報酬はいくらかを検証し、新しいブロックを生成します。この仕組みにより、AIエージェントがサーバーハックを試みるなどの攻撃に対しても、分散システムとして耐性を持たせることができます。

なぜブロックチェーンを使うのかという点について補足させてください。AIエージェントは中央集権的なサーバーを攻撃対象として認識する可能性があります。分散システムであれば、攻撃対象を分散させることができ、長期的にセキュリティ面で有利だという仮説を持っています。また、最終的にAIエージェント経済が実現する舞台は公開ブロックチェーンになると考えており、その前段階としてプライベートチェーンで実験を行います。

このアーキテクチャの設計について、Soneiumとの統合可能性も含めてご相談できればと思っています。
-->
