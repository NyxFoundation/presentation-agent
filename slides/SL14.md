---
layout: default
---

# どの関数のどの行が間違っていたか

<div class="text-xs opacity-60 mb-2">Current Finance ／ <span class="font-mono">market.move handle_debt_auto_deleverage</span> ／ Sherlock contest #312</div>

<img :src="'/images/diagrams/sl14_attack.svg'" class="w-full mx-auto block mt-1" style="max-height: 420px;" alt=":580 だけが粒度を間違えた ─ 3つの管理ポイント比較" />

<!--
前ページのユーザシナリオを同じ絵で再掲し、今度はコード側を重ねます。問題関数はhandle_debt_auto_deleverage、market.moveの546行目から始まる。左の絵で、:580は中央の黒い金庫全体を読んでしまった。本来は:686と同じ書き方で、emode_groupからGroup Aの借入だけを読むべきだった。右の3パネル。575行登録は emode_group_id を渡してグループ別、正しい。580行実行ガードはself.reserves.load_by_type().debt()でreserve全体を読んでしまい、これがBUG。686行停止判定はemode_group.borrow_amount()でグループ別を読む、正しい。3箇所のうち580行だけが粒度を間違えた。直し方は.debt()を.borrow_amount(emode_group_id)に変える、これだけ。皆様の言葉に翻訳すると、顧客別の与信枠を3箇所で参照しているのに、強制執行トリガーだけが銀行全体の融資残高と比較してしまった。ITGC-CC04やFISC統83に相当する、コントロール粒度の不整合です。
-->
