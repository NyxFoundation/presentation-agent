---
layout: cover
---
<div class="cover-stage">
  <div class="nx-grid-bg"></div>
  <div class="cover-inner">
    <h1 class="cover-build">
      Build <em>verifiable trust</em><br/>with us.
    </h1>
    <div class="cover-rule"></div>
    <div class="cover-wordmark">
      <div class="cw-en">Nyx Foundation</div>
      <div class="cw-jp">一般社団法人 Nyx Foundation</div>
    </div>
    <div class="cover-links">
      <span>contact@nyx.foundation</span>
      <span class="cs-sep">·</span>
      <span>nyx.foundation</span>
    </div>
  </div>
</div>

<style>
.cover-stage { position: absolute; inset: 0; background: var(--bg); overflow: hidden; }
.cover-inner {
  position: relative; z-index: 1; height: 100%;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  text-align: center; padding: 2rem;
}
.cover-status {
  display: flex; align-items: center; gap: 12px;
  font-family: var(--font-mono); font-size: 14px; font-weight: 500;
  letter-spacing: 0.18em; text-transform: uppercase; color: var(--ink-dim);
  margin-bottom: 1.8rem;
}
.cs-sep { color: var(--ink-faint); }

.cover-build {
  font-family: var(--font-serif);
  font-weight: 300;
  font-size: 64px;
  line-height: 1.1;
  letter-spacing: -0.015em;
  color: var(--ink);
  margin: 0 0 1.4rem;
  text-wrap: balance;
  border-bottom: none !important;
  padding-bottom: 0 !important;
  display: block !important;
}
.cover-build em {
  font-style: italic;
  font-weight: 300;
  color: var(--accent);
}

.cover-thesis {
  font-family: var(--font-jp-serif); font-size: 19px; font-weight: 500;
  letter-spacing: 0.03em; color: var(--ink-dim); line-height: 1.5;
}
.cover-thesis em {
  font-style: normal; color: var(--accent);
  border-bottom: 1px solid var(--accent-line); padding-bottom: 1px;
}

.cover-rule {
  width: 56px; height: 1px; background: var(--line-strong);
  margin: 1.8rem auto 1rem;
}

.cover-wordmark { margin-bottom: 0.7rem; }
.cw-en {
  font-family: var(--font-wordmark) !important; font-size: 22px; font-weight: 400;
  letter-spacing: 0.02em; color: var(--ink); line-height: 1.1;
}
.cw-jp {
  font-family: var(--font-wordmark) !important; font-size: 13px; letter-spacing: 0.22em;
  color: var(--ink-faint); margin-top: 0.35rem;
}

.cover-links {
  display: flex; gap: 14px;
  font-family: var(--font-mono); font-size: 13px;
  letter-spacing: 0.14em; color: var(--ink-dim);
  margin-top: 0.55rem;
}
</style>

<!--
Speaker Notes:
最後に。検証できる信頼は、ひとつの製品の話ではありません。社会が、これから何を信じるのかという話です。いますぐ何かを決める必要はありません。けれど、これから社会の信頼がどこから生まれるのかを考えるなら、ここは見ておくべき場所です。寄付でも、スポンサーでも、共同研究でも、どの関わり方でも構いません。nyx.foundation でお待ちしています。ありがとうございました。
-->
